import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Appbar = ({title,button}) => {
  return (
    <View>
      <Text>Appbar</Text>
    </View>
  )
}

export default Appbar

const styles = StyleSheet.create({})